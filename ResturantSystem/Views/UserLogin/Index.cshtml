@model ResturantSystem.Models.Enroll

@{
    ViewBag.Title = "ENROLLMENT";
    Layout = "~/Views/Shared/_Layout.cshtml";


}


<!------ Include the above in your HEAD tag ---------->
@using (Html.BeginForm("Index", "UserLogin", FormMethod.Post))
{
@Html.AntiForgeryToken()  
<div class="container register" style="padding-top: 100px">
                <div class="row">
                    <div class="col-md-3 register-left">
                    </div>
                    <div class="col-md-9 register-right">
                        <ul class="nav nav-tabs nav-justified" id="myTab" role="tablist">
                            <li class="nav-item">
                                <a class="nav-link " id="home-tab"  href="@Url.Action("Index","Enrollment/Index")" role="tab" aria-controls="home" aria-selected="true">Register</a>
                            </li>
                            <li class="nav-item">
                                <a class="nav-link active" id="profile-tab"  href="@Url.Action("Index","UserLogin")" role="tab" aria-controls="profile" aria-selected="false">Login</a>
                            </li>
                        </ul>
                        <div class="tab-content" id="myTabContent">
                            <div class="tab-pane fade show active" id="home" role="tabpanel" aria-labelledby="home-tab">
                                <h3 class="register-heading">Login here</h3>
                                <div class="row register-form">
                                    <div class="col-md-6">
                                       <div class="form-group">
                                            @Html.TextBoxFor(e => e.UserName, new { @class = "form-control", placeholder = "Your UserName *" })
                                            @Html.ValidationMessageFor(e => e.UserName)
                                        </div>
                                        <div class="form-group">
                                           @Html.PasswordFor(e => e.Password, new { id = "password", @class = "form-control", placeholder = "Password *" })
                                             <span id="result"></span>
                                             @Html.ValidationMessageFor(e => e.Password)
                                        </div>
                                    <div class="col-md-6">
                                         <input type="submit" class="btnLogin"  value="Login"/>
                                    </div>
                                        
                                    </div>
                                    
                                </div>
                            </div>
                         </div>
                    </div>
                </div>
               
<script type="text/javascript">
$(function () {
var msg = '@ViewData["result"]';
if (msg == '1')
{
    alert("User Details Inserted Successfully");
    window.location.href = "@Url.Action("Index", "UserLogin")";
    }

});
</script>

</div>
<script type="text/javascript">

    $(document).ready(function () {
        $.ajax({

            type: 'POST',
            URL: '/UserLogin/Index',
            data: data,
            dataType: 'JSON',
            success: function (data) {
                data = data.status
                if (status == "1") {
                    window.location.href = '@Url.Action("Index", "UserLogin")';
                }
            }
        });
    });
</script>
}


